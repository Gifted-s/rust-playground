/// Adds one to the number given
///
/// # Examples
///
/// ```
/// let args = 5;
/// let answer = mycrate::add_one(arg);
///
/// assert_eq!(6, answer);
/// ```

fn main() {}

pub fn add_one(x: i32) -> i32 {
    x + 1
}
